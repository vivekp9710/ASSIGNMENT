#include<stdio.h>
main(){
	char name[40],address[40];
	int age,birthdate;
	printf("enter the name:");
	scanf("%s",&name);
	printf("\nenter the address:");
	scanf("%s",&address);
	printf("\nenter the age:");
	scanf("%d",&age);
	printf("\nenter the birthdate:");
	scanf("%d",&birthdate);
}

